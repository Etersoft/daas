#!/bin/sh

usage()
{
	echo "daas compose [-h | commands ]"
}

[ -z "$1" ] && usage && exit 1

[ "$1" == "compose" ] && shift && daas-docker-compose-gen.py "$@"

# [ "$1" == "ansible" ] && shift && daas-docker-ansiblbe.py "$@"
